// Student applications. A tarbiyachi can view and approve/reject a pending
// application (same body as /dekan/murojaatlar) but cannot delete one —
// the trash control is hidden and the status picker is limited to
// approve/reject (see useStaffPanel + /api/admin/arizalar).
export { default } from '@/app/admin/arizalar/page'

// The tarbiyachi dashboard is the dekan dashboard, faculty-scoped and
// read-only (see useStaffPanel). All the KPI cards and feeds are the same
// building's data; the only difference is the in-panel links resolve to
// `/tarbiyachi/*`.
export { default } from '@/app/dekan/dashboard/page'

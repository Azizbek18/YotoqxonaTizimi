// The student directory, view-only for a tarbiyachi (edit / delete /
// warning / blacklist controls are hidden — see useStaffPanel). All the
// read-only detail tabs, filters and payment history stay.
export { default } from '@/app/dekan/talabalar/page'

// Moved to components/ui/skeletons.tsx — shared across every panel now.
// Re-exported here so existing `@/components/dekan/Skeletons` imports keep
// working.
export * from '@/components/ui/skeletons'
